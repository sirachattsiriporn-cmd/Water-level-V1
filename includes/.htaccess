# ไฟล์: /configs/.htaccess และ /includes/.htaccess

# สั่งปิดตาย! ห้าม Browser เรียกไฟล์ในห้องนี้โดยตรง
# (แต่ PHP ใน index.php ยังดึงข้อมูลไปใช้ได้ปกติ)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
