# ไฟล์: /.htaccess (ตัวหลัก)

# 1. ปิดไม่ให้เห็นรายชื่อไฟล์เวลากดเข้าโฟลเดอร์เปล่าๆ
Options -Indexes

# 2. ป้องกันไม่ให้ใครมาแอบอ่านไฟล์ .htaccess นี้
<Files .htaccess>
    Order allow,deny
    Deny from all
</Files>